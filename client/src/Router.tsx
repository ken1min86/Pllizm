import { Route, Switch } from 'react-router';

const Router = () => {
  return (
    <Switch>
      <Route exact path={'/'}>
        {/* コンポーネント */}
      </Route>
    </Switch>
  );
};

export default Router;
